// Seed data: the name of the first workspace `database/seed.ts` creates (overridable with
// SEED_WORKSPACE_NAME). The app never reads this.
export const DEFAULT_WORKSPACE_NAME = 'Default workspace';
