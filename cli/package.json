{
  "name": "simple-auth-kit-cli",
  "private": true,
  "version": "0.0.0",
  "description": "simple-auth-kit add <combo> — copies registry source into a consumer repo. Not installed as a runtime dependency by anyone. Run `npm link` once inside this directory to get a global `simple-auth-kit` command (shadcn-CLI-style); or invoke directly with `npx tsx simple-auth-kit.ts add <combo>`.",
  "bin": {
    "simple-auth-kit": "./bin/simple-auth-kit.mjs"
  },
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "cli": "tsx simple-auth-kit.ts"
  },
  "dependencies": {
    "prompts": "^2.4.2"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "@types/prompts": "^2.4.9",
    "tsx": "^4.23.11",
    "typescript": "^7.0.2"
  }
}
