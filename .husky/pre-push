# Local (no-CI) enforcement of the prove-cycle gate: git invokes hooks with an unspecified cwd,
# so resolve the repo root explicitly rather than assuming this runs from the top.
cd "$(git rev-parse --show-toplevel)"
make prove-changed
